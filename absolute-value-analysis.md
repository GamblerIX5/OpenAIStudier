# 函数 $f(x) = |x|$ 的极值分析

## 1. 函数定义

$$f(x) = |x| = \begin{cases} x, & x \geq 0 \\ -x, & x < 0 \end{cases}$$

## 2. 图像

```
     f(x)
      |
      |     /
      |    /
      |   /
      |  /
      | /
------+-------- x
     /|
    / |
   /  |
```

## 3. 极值分析

### 3.1 极小值

**结论**：在 $x = 0$ 处取得**全局极小值**，极小值为 $f(0) = 0$。

**证明**：
- 对任意 $x \in \mathbb{R}$，有 $|x| \geq 0$
- 当且仅当 $x = 0$ 时，$|x| = 0$
- 因此 $f(0) = 0$ 是全局最小值

### 3.2 极大值

**结论**：**无极大值**。

**理由**：
- 当 $x \to +\infty$ 时，$f(x) \to +\infty$
- 当 $x \to -\infty$ 时，$f(x) \to +\infty$
- 函数无上界，故不存在极大值

## 4. 可导性分析

在 $x = 0$ 处：
- 左导数：$\lim_{h \to 0^-} \frac{f(0+h) - f(0)}{h} = \lim_{h \to 0^-} \frac{-h}{h} = -1$
- 右导数：$\lim_{h \to 0^+} \frac{f(0+h) - f(0)}{h} = \lim_{h \to 0^+} \frac{h}{h} = 1$

由于左导数 $\neq$ 右导数，**$f(x)$ 在 $x = 0$ 处不可导**。

## 5. 总结

| 项目 | 结论 |
|------|------|
| 极小值点 | $x = 0$ |
| 极小值 | $0$ |
| 极大值点 | 无 |
| 是否可导（在极值点） | 否 |

> **注意**：极值点的存在不依赖于函数在该点是否可导。$f(x) = |x|$ 在 $x = 0$ 处不可导，但仍取得极小值。
