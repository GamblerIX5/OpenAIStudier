---
title: CLI
description: أوامر وخيارات CLI في OpenStudier.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

يبدأ CLI الخاص بـ OpenStudier افتراضيا واجهة [TUI](/docs/tui) عند تشغيله بدون أي معاملات.

```bash
openstudier
```

كما يدعم أيضا أوامر موثقة في هذه الصفحة، ما يتيح لك التفاعل مع OpenStudier برمجيا.

```bash
openstudier run "Explain how closures work in JavaScript"
```

---

### tui

بدء واجهة المستخدم terminal (TUI) الخاصة بـ OpenStudier.

```bash
openstudier [project]
```

#### الخيارات

| الخيار       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--prompt`   |         | الموجّه المراد استخدامه                                           |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--port`     |         | المنفذ الذي يتم الاستماع عليه                                     |
| `--hostname` |         | اسم المضيف الذي يتم الاستماع عليه                                 |

---

## الأوامر

يتضمن CLI الخاص بـ OpenStudier أيضا الأوامر التالية.

---

### agent

إدارة وكلاء OpenStudier.

```bash
openstudier agent [command]
```

---

### attach

إرفاق terminal بخادم الواجهة الخلفية لـ OpenStudier قيد التشغيل بالفعل، والذي تم تشغيله عبر الأمرين `serve` أو `web`.

```bash
openstudier attach [url]
```

يتيح ذلك استخدام واجهة TUI مع واجهة خلفية لـ OpenStudier تعمل عن بعد. على سبيل المثال:

```bash
# ابدأ خادم الواجهة الخلفية للوصول عبر الويب/الجوال
openstudier web --port 4096 --hostname 0.0.0.0

# في محطة طرفية (terminal) أخرى، اربط TUI بالواجهة الخلفية قيد التشغيل
openstudier attach http://10.20.30.40:4096
```

#### الرايات

| الراية      | المختصر | الوصف                               |
| ----------- | ------- | ----------------------------------- |
| `--dir`     |         | دليل العمل الذي ستبدأ منه واجهة TUI |
| `--session` | `-s`    | معرّف الجلسة للمتابعة               |

---

#### create

إنشاء وكيل جديد بإعدادات مخصصة.

```bash
openstudier agent create
```

سيرشدك هذا الأمر خلال إنشاء وكيل جديد مع موجه نظام مخصص وإعدادات الأدوات.

---

#### list

عرض جميع الوكلاء المتاحين.

```bash
openstudier agent list
```

---

### auth

أمر لإدارة بيانات الاعتماد وتسجيل الدخول لمزودي الخدمة.

```bash
openstudier auth [command]
```

---

#### login

يعتمد OpenStudier على قائمة المزودين في [Models.dev](https://models.dev)، لذا يمكنك استخدام `openstudier auth login` لتهيئة مفاتيح API لأي مزود ترغب باستخدامه. يتم حفظ ذلك في `~/.local/share/openstudier/auth.json`.

```bash
openstudier auth login
```

عند تشغيل OpenStudier يقوم بتحميل المزودين من ملف بيانات الاعتماد، وكذلك أي مفاتيح معرّفة في متغيرات البيئة لديك أو في ملف `.env` ضمن مشروعك.

---

#### list

يسرد جميع المزودين الذين تم توثيقهم كما هم محفوظون في ملف بيانات الاعتماد.

```bash
openstudier auth list
```

أو النسخة المختصرة.

```bash
openstudier auth ls
```

---

#### logout

يسجلك خارج مزود عبر حذفه من ملف بيانات الاعتماد.

```bash
openstudier auth logout
```

---

### github

إدارة وكيل GitHub لأتمتة المستودع.

```bash
openstudier github [command]
```

---

#### install

تثبيت وكيل GitHub في مستودعك.

```bash
openstudier github install
```

يقوم ذلك بإعداد سير عمل GitHub Actions اللازم ويرشدك خلال عملية التهيئة. [اعرف المزيد](/docs/github).

---

#### run

تشغيل وكيل GitHub. يُستخدم هذا عادة ضمن GitHub Actions.

```bash
openstudier github run
```

##### الرايات

| الراية    | الوصف                                |
| --------- | ------------------------------------ |
| `--event` | حدث GitHub مُحاكى لتشغيل الوكيل عليه |
| `--token` | رمز وصول شخصي لـ GitHub              |

---

### mcp

إدارة خوادم Model Context Protocol.

```bash
openstudier mcp [command]
```

---

#### add

إضافة خادم MCP إلى تهيئتك.

```bash
openstudier mcp add
```

سيرشدك هذا الأمر خلال إضافة خادم MCP محلي أو بعيد.

---

#### list

عرض جميع خوادم MCP المُهيأة وحالة اتصالها.

```bash
openstudier mcp list
```

أو استخدم النسخة المختصرة.

```bash
openstudier mcp ls
```

---

#### auth

إجراء المصادقة مع خادم MCP يدعم OAuth.

```bash
openstudier mcp auth [name]
```

إذا لم تُحدِّد اسم خادم، فسيُطلب منك الاختيار من الخوادم المتاحة الداعمة لـ OAuth.

يمكنك أيضا عرض الخوادم الداعمة لـ OAuth وحالة المصادقة الخاصة بها.

```bash
openstudier mcp auth list
```

أو استخدم النسخة المختصرة.

```bash
openstudier mcp auth ls
```

---

#### logout

إزالة بيانات اعتماد OAuth لخادم MCP.

```bash
openstudier mcp logout [name]
```

---

#### debug

تشخيص مشاكل اتصال OAuth لخادم MCP.

```bash
openstudier mcp debug <name>
```

---

### models

عرض جميع النماذج المتاحة من المزودين المُهيأين.

```bash
openstudier models [provider]
```

يعرض هذا الأمر جميع النماذج المتاحة عبر المزودين المُهيأين لديك بصيغة `provider/model`.

هذا مفيد لتحديد اسم النموذج الدقيق الذي ستستخدمه في [تهيئتك](/docs/config/).

يمكنك اختياريا تمرير معرّف مزود لتصفية النماذج حسب ذلك المزود.

```bash
openstudier models anthropic
```

#### الرايات

| الراية      | الوصف                                                         |
| ----------- | ------------------------------------------------------------- |
| `--refresh` | تحديث ذاكرة التخزين المؤقت للنماذج من models.dev              |
| `--verbose` | استخدام مخرجات أكثر تفصيلا للنماذج (تشمل بيانات مثل التكاليف) |

استخدم الراية `--refresh` لتحديث قائمة النماذج المخزنة مؤقتا. يفيد ذلك عند إضافة نماذج جديدة إلى مزود وتريد رؤيتها في OpenStudier.

```bash
openstudier models --refresh
```

---

### run

تشغيل openstudier في وضع غير تفاعلي عبر تمرير موجه مباشرة.

```bash
openstudier run [message..]
```

هذا مفيد للبرمجة النصية والأتمتة، أو عندما تريد إجابة سريعة دون تشغيل واجهة TUI كاملة. على سبيل المثال.

```bash "openstudier run"
openstudier run Explain the use of context in Go
```

يمكنك أيضا الإرفاق بمثيل `openstudier serve` قيد التشغيل لتجنّب زمن الإقلاع البارد لخوادم MCP في كل تشغيل:

```bash
# ابدأ خادمًا بلا واجهة في محطة طرفية واحدة
openstudier serve

# في محطة طرفية أخرى، شغّل الأوامر التي ترتبط به
openstudier run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### الرايات

| الراية       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--command`  |         | الأمر المراد تشغيله؛ استخدم الرسالة كوسائط                        |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--share`    |         | مشاركة الجلسة                                                     |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--file`     | `-f`    | ملف/ملفات لإرفاقها بالرسالة                                       |
| `--format`   |         | التنسيق: default (منسق) أو json (أحداث JSON خام)                  |
| `--title`    |         | عنوان للجلسة (يستخدم موجهًا مقتطعًا إن لم تُحدَّد قيمة)           |
| `--attach`   |         | الإرفاق بخادم openstudier قيد التشغيل (مثل http://localhost:4096)    |
| `--port`     |         | منفذ الخادم المحلي (الافتراضي منفذ عشوائي)                        |

---

### serve

بدء خادم OpenStudier بدون واجهة للوصول عبر API. راجع [توثيق الخادم](/docs/server) لواجهة HTTP الكاملة.

```bash
openstudier serve
```

يشغّل هذا خادم HTTP يوفّر وصولا عبر API لوظائف openstudier دون واجهة TUI. اضبط `OPENSTUDIER_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `openstudier`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### session

إدارة جلسات OpenStudier.

```bash
openstudier session [command]
```

---

#### list

عرض جميع جلسات OpenStudier.

```bash
openstudier session list
```

##### الرايات

| الراية        | المختصر | الوصف                                 |
| ------------- | ------- | ------------------------------------- |
| `--max-count` | `-n`    | حصر النتائج في أحدث N جلسات           |
| `--format`    |         | تنسيق المخرجات: table أو json (table) |

---

### stats

عرض إحصاءات استخدام الرموز والتكلفة لجلسات OpenStudier لديك.

```bash
openstudier stats
```

#### الرايات

| الراية      | الوصف                                                                     |
| ----------- | ------------------------------------------------------------------------- |
| `--days`    | عرض الإحصاءات لآخر N يومًا (الافتراضي: كل الوقت)                          |
| `--tools`   | عدد الأدوات المطلوب عرضها (الافتراضي: الكل)                               |
| `--models`  | عرض تفصيل استخدام النماذج (مخفي افتراضيا). مرّر رقمًا لعرض أعلى N         |
| `--project` | التصفية حسب المشروع (الافتراضي: كل المشاريع، سلسلة فارغة: المشروع الحالي) |

---

### export

تصدير بيانات الجلسة بصيغة JSON.

```bash
openstudier export [sessionID]
```

إذا لم تُقدّم معرّف جلسة، فسيُطلب منك الاختيار من الجلسات المتاحة.

---

### import

استيراد بيانات الجلسة من ملف JSON أو رابط مشاركة OpenStudier.

```bash
openstudier import <file>
```

يمكنك الاستيراد من ملف محلي أو من رابط مشاركة OpenStudier.

```bash
openstudier import session.json
openstudier import https://opncd.ai/s/abc123
```

---

### web

بدء خادم OpenStudier بدون واجهة مع واجهة ويب.

```bash
openstudier web
```

يشغّل هذا خادم HTTP ويفتح متصفح ويب للوصول إلى OpenStudier عبر واجهة ويب. اضبط `OPENSTUDIER_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `openstudier`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### acp

بدء خادم ACP (Agent Client Protocol).

```bash
openstudier acp
```

يشغّل هذا الأمر خادم ACP يتواصل عبر stdin/stdout باستخدام nd-JSON.

#### الرايات

| الراية       | الوصف                             |
| ------------ | --------------------------------- |
| `--cwd`      | دليل العمل                        |
| `--port`     | المنفذ الذي يتم الاستماع عليه     |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه |

---

### uninstall

إلغاء تثبيت OpenStudier وإزالة جميع الملفات ذات الصلة.

```bash
openstudier uninstall
```

#### الرايات

| الراية          | المختصر | الوصف                               |
| --------------- | ------- | ----------------------------------- |
| `--keep-config` | `-c`    | الإبقاء على ملفات التهيئة           |
| `--keep-data`   | `-d`    | الإبقاء على بيانات الجلسات واللقطات |
| `--dry-run`     |         | عرض ما سيتم حذفه دون تنفيذ الحذف    |
| `--force`       | `-f`    | تخطي مطالبات التأكيد                |

---

### upgrade

تحديث openstudier إلى أحدث إصدار أو إلى إصدار محدد.

```bash
openstudier upgrade [target]
```

للترقية إلى أحدث إصدار.

```bash
openstudier upgrade
```

للترقية إلى إصدار محدد.

```bash
openstudier upgrade v0.1.48
```

#### الرايات

| الراية     | المختصر | الوصف                                                       |
| ---------- | ------- | ----------------------------------------------------------- |
| `--method` | `-m`    | طريقة التثبيت المستخدمة: curl أو npm أو pnpm أو bun أو brew |

---

## الخيارات العامة

يدعم سطر أوامر openstudier الخيارات العامة التالية.

| الراية         | المختصر | الوصف                                  |
| -------------- | ------- | -------------------------------------- |
| `--help`       | `-h`    | عرض المساعدة                           |
| `--version`    | `-v`    | طباعة رقم الإصدار                      |
| `--print-logs` |         | طباعة السجلات إلى stderr               |
| `--log-level`  |         | مستوى السجل (DEBUG, INFO, WARN, ERROR) |

---

## متغيرات البيئة

يمكن تهيئة OpenStudier باستخدام متغيرات البيئة.

| المتغير                               | النوع   | الوصف                                                     |
| ------------------------------------- | ------- | --------------------------------------------------------- |
| `OPENSTUDIER_AUTO_SHARE`                 | boolean | مشاركة الجلسات تلقائيا                                    |
| `OPENSTUDIER_GIT_BASH_PATH`              | string  | مسار ملف Git Bash التنفيذي على Windows                    |
| `OPENSTUDIER_CONFIG`                     | string  | مسار ملف التهيئة                                          |
| `OPENSTUDIER_CONFIG_DIR`                 | string  | مسار دليل التهيئة                                         |
| `OPENSTUDIER_CONFIG_CONTENT`             | string  | محتوى تهيئة JSON مُضمّن                                   |
| `OPENSTUDIER_DISABLE_AUTOUPDATE`         | boolean | تعطيل التحقق التلقائي من التحديثات                        |
| `OPENSTUDIER_DISABLE_PRUNE`              | boolean | تعطيل تنقية البيانات القديمة                              |
| `OPENSTUDIER_DISABLE_TERMINAL_TITLE`     | boolean | تعطيل تحديث عنوان terminal تلقائيا                        |
| `OPENSTUDIER_PERMISSION`                 | string  | تهيئة أذونات JSON مُضمّنة                                 |
| `OPENSTUDIER_DISABLE_DEFAULT_PLUGINS`    | boolean | تعطيل الإضافات الافتراضية                                 |
| `OPENSTUDIER_DISABLE_LSP_DOWNLOAD`       | boolean | تعطيل تنزيل خوادم LSP تلقائيا                             |
| `OPENSTUDIER_ENABLE_EXPERIMENTAL_MODELS` | boolean | تفعيل النماذج التجريبية                                   |
| `OPENSTUDIER_DISABLE_AUTOCOMPACT`        | boolean | تعطيل ضغط السياق تلقائيا                                  |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE`        | boolean | تعطيل القراءة من `.claude` (الموجه + المهارات)            |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | تعطيل قراءة `~/.claude/CLAUDE.md`                         |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | تعطيل تحميل `.claude/skills`                              |
| `OPENSTUDIER_DISABLE_MODELS_FETCH`       | boolean | تعطيل جلب النماذج من مصادر بعيدة                          |
| `OPENSTUDIER_FAKE_VCS`                   | string  | مزود VCS وهمي لأغراض الاختبار                             |
| `OPENSTUDIER_DISABLE_FILETIME_CHECK`     | boolean | تعطيل التحقق من وقت الملف لتحسين الأداء                   |
| `OPENSTUDIER_CLIENT`                     | string  | معرّف العميل (الافتراضي `cli`)                            |
| `OPENSTUDIER_ENABLE_EXA`                 | boolean | تفعيل أدوات بحث الويب من Exa                              |
| `OPENSTUDIER_SERVER_PASSWORD`            | string  | تفعيل المصادقة الأساسية لخادمي `serve`/`web`              |
| `OPENSTUDIER_SERVER_USERNAME`            | string  | تجاوز اسم مستخدم المصادقة الأساسية (الافتراضي `openstudier`) |
| `OPENSTUDIER_MODELS_URL`                 | string  | رابط مخصص لجلب تهيئة النماذج                              |

---

### تجريبي

تقوم متغيرات البيئة التالية بتفعيل ميزات تجريبية قد تتغير أو تتم إزالتها.

| المتغير                                         | النوع   | الوصف                                       |
| ----------------------------------------------- | ------- | ------------------------------------------- |
| `OPENSTUDIER_EXPERIMENTAL`                         | boolean | تفعيل جميع الميزات التجريبية                |
| `OPENSTUDIER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | تفعيل اكتشاف الأيقونات                      |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | تعطيل النسخ عند التحديد في TUI              |
| `OPENSTUDIER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | المهلة الافتراضية لأوامر bash بالميلي ثانية |
| `OPENSTUDIER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | الحد الأقصى لرموز المخرجات لاستجابات LLM    |
| `OPENSTUDIER_EXPERIMENTAL_FILEWATCHER`             | boolean | تفعيل مراقب الملفات لكامل الدليل            |
| `OPENSTUDIER_EXPERIMENTAL_OXFMT`                   | boolean | تفعيل مُنسّق oxfmt                          |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TOOL`                | boolean | تفعيل أداة LSP تجريبية                      |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | تعطيل مراقب الملفات                         |
| `OPENSTUDIER_EXPERIMENTAL_EXA`                     | boolean | تفعيل ميزات Exa التجريبية                   |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TY`                  | boolean | تفعيل تدقيق أنواع LSP تجريبي                |
| `OPENSTUDIER_EXPERIMENTAL_MARKDOWN`                | boolean | تفعيل ميزات markdown تجريبية                |
| `OPENSTUDIER_EXPERIMENTAL_PLAN_MODE`               | boolean | تفعيل وضع الخطة                             |
