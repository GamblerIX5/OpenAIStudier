---
title: CLI
description: OpenStudier CLI muligheder og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

OpenStudier CLI starter som standard [TUI](/docs/tui), når den kører uden nogen argumenter.

```bash
openstudier
```

Men den accepterer også kommandoer som dokumenteret på denne side. Dette giver dig mulighed for at interagere med OpenStudier programmatisk.

```bash
openstudier run "Explain how closures work in JavaScript"
```

---

### tui

Start OpenStudier terminalbrugergrænsefladen.

```bash
openstudier [project]
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                  |
| ------------ | ---- | ---------------------------------------------------------------------------- |
| `--continue` | `-c` | Fortsæt sidste session                                                       |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                 |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`) |
| `--prompt`   |      | Spørg om at bruge                                                            |
| `--model`    | `-m` | Model til brug i form af provider/model                                      |
| `--agent`    |      | Agent hos bruge                                                              |
| `--port`     |      | Port at lytte på                                                             |
| `--hostname` |      | Værtsnavn at lytte på                                                        |

---

## Kommandoer

OpenStudier CLI har også følgende kommandoer.

---

### agent

Administrer agenter i OpenStudier.

```bash
openstudier agent [command]
```

---

### attach

Tilslut en terminal til en allerede kørende OpenStudier backend-server startet via `serve` eller `web` kommandoer.

```bash
openstudier attach [url]
```

Dette gør det muligt at bruge TUI med en ekstern OpenStudier backend. For eksempel:

```bash
# Start the backend server for web/mobile access
openstudier web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
openstudier attach http://10.20.30.40:4096
```

#### Flag

| Flag        | Kort | Beskrivelse                      |
| ----------- | ---- | -------------------------------- |
| `--dir`     |      | Arbejdsmappe til at starte TUI i |
| `--session` | `-s` | Sessions-id for at fortsætte     |

---

#### create

Opret en ny agent med brugerdefineret konfiguration.

```bash
openstudier agent create
```

Denne kommando vil guide dig gennem oprettelse af en ny agent med en brugerdefineret systemprompt og værktøjskonfiguration.

---

#### list

Liste over alle tilgængelige agenter.

```bash
openstudier agent list
```

---

### auth

Kommando til at administrere legitimationsoplysninger og login for udbydere.

```bash
openstudier auth [command]
```

---

#### login

OpenStudier drives af udbyderlisten på [Models.dev](https://models.dev), så du kan bruge `openstudier auth login` til at konfigurere API-nøgler for enhver udbyder, du vil bruge. Dette er gemt i `~/.local/share/openstudier/auth.json`.

```bash
openstudier auth login
```

Når OpenStudier starter op, indlæser den udbyderne fra legitimationsfilen. Og hvis der er nogen nøgler defineret i dine miljøer eller en `.env` fil i dit projekt.

---

#### list

Viser alle de godkendte udbydere som gemt i legitimationsfilen.

```bash
openstudier auth list
```

Eller den korte version.

```bash
openstudier auth ls
```

---

#### logout

Logger dig ud af en udbyder ved at rydde den fra legitimationsfilen.

```bash
openstudier auth logout
```

---

### github

Administrator GitHub-agenten til lagerautomatisering.

```bash
openstudier github [command]
```

---

#### install

Installatør GitHub-agenten i dit lager.

```bash
openstudier github install
```

Dette opsætter det nødvendige GitHub Actions workflow og guider dig gennem konfigurationsprocessen. [Learn more](/docs/github).

---

#### run

Kør GitHub-agenten. Dette bruges typisk i GitHub Actions.

```bash
openstudier github run
```

##### Flag

| Flag      | Beskrivelse                                    |
| --------- | ---------------------------------------------- |
| `--event` | GitHub mock begivenhed for at køre agenten for |
| `--token` | GitHub personlig adgangstoken                  |

---

### mcp

Administrator Model Context Protocol-servere.

```bash
openstudier mcp [command]
```

---

#### add

Tilføj en MCP-server til din konfiguration.

```bash
openstudier mcp add
```

Denne kommando vil guide dig gennem tilføjelse af enten en lokal eller ekstern MCP-server.

---

#### list

Liste over alle konfigurerede MCP-servere og deres forbindelsesstatus.

```bash
openstudier mcp list
```

Eller brug den korte version.

```bash
openstudier mcp ls
```

---

#### auth

Godkendt med en OAuth-aktiveret MCP-server.

```bash
openstudier mcp auth [name]
```

Hvis du ikke angiver et servernavn, bliver du bedt om at vælge blandt tilgængelige OAuth-kompatible servere.

Du kan også angive OAuth-kompatible servere og deres godkendelsesstatus.

```bash
openstudier mcp auth list
```

Eller brug den korte version.

```bash
openstudier mcp auth ls
```

---

#### logout

Fjern OAuth-legitimationsoplysninger for en MCP-server.

```bash
openstudier mcp logout [name]
```

---

#### debug

Fejlfinding af OAuth-forbindelsesproblemer for en MCP-server.

```bash
openstudier mcp debug <name>
```

---

### models

Liste over alle tilgængelige modeller fra konfigurerede udbydere.

```bash
openstudier models [provider]
```

Denne kommando viser alle tilgængelige modeller på tværs af dine konfigurerede udbydere i formatet `provider/model`.

Dette er nyttigt til at finde ud af det nøjagtige modelnavn, der skal bruges i [your config](/docs/config/).

Du kan eventuelt videregive et udbyder-id for at filtrere modeller efter den udbyder.

```bash
openstudier models anthropic
```

#### Flag

| Flag        | Beskrivelse                                                             |
| ----------- | ----------------------------------------------------------------------- |
| `--refresh` | Opdater modelcachen fra models.dev                                      |
| `--verbose` | Brug mere detaljeret modeloutput (inkluderer metadata som omkostninger) |

Brug flaget `--refresh` til at opdatere den cachelagrede modelliste. Dette er nyttigt, når nye modeller er blevet tilføjet til en udbyder, og du vil se dem i OpenStudier.

```bash
openstudier models --refresh
```

---

### run

Kør openstudier i ikke-interaktiv tilstand ved at sende en prompt direkte.

```bash
openstudier run [message..]
```

Dette er nyttigt til scripting, automatisering, eller når du vil have et hurtigt svar uden at starte hele TUI. F.eks.

```bash "openstudier run"
openstudier run Explain the use of context in Go
```

Du kan også vedhæfte til en kørende `openstudier serve`-instans for at undgå MCP serverens kolde opstartstider ved hver kørsel:

```bash
# Start a headless server in one terminal
openstudier serve

# In another terminal, run commands that attach to it
openstudier run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                         |
| ------------ | ---- | ----------------------------------------------------------------------------------- |
| `--command`  |      | Kommandoen til at køre, brug besked til args                                        |
| `--continue` | `-c` | Fortsæt sidste session                                                              |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                        |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`)        |
| `--share`    |      | Del sessionen                                                                       |
| `--model`    | `-m` | Model til brug i form af provider/model                                             |
| `--agent`    |      | Agent til brug                                                                      |
| `--file`     | `-f` | Fil(er), der skal vedhæftes til meddelelsen                                         |
| `--format`   |      | Format: standard (formateret) eller json (rå JSON hændelser)                        |
| `--title`    |      | Titel for sessionen (bruger trunkeret prompt, hvis der ikke er angivet nogen værdi) |
| `--attach`   |      | Tilslut til en kørende openstudier-server (f.eks. http://localhost:4096)               |
| `--port`     |      | Port til den lokale server (standard til vilkårlig port)                            |

---

### serve

Start en hovedløs OpenStudier-server til API-adgang. Tjek [server docs](/docs/server) for den fulde HTTP-grænseflade.

```bash
openstudier serve
```

Dette starter en HTTP-server, der giver API-adgang til openstudier-funktionalitet uden TUI-grænsefladen. Indstil `OPENSTUDIER_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `openstudier`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### session

Administrator OpenStudier sessionsholder.

```bash
openstudier session [command]
```

---

#### list

Liste over alle OpenStudier sessioner.

```bash
openstudier session list
```

##### Flag

| Flag          | Kort | Beskrivelse                            |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | Begræns til N seneste sessioner        |
| `--format`    |      | Outputformat: tabel eller json (tabel) |

---

### stats

Vis tokenbrug og omkostningsstatistikker for dine OpenStudier-sessioner.

```bash
openstudier stats
```

#### Flag

| Flag        | Beskrivelse                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Vis statistik for de sidste N dage (hele tiden)                             |
| `--tools`   | Antal værktøjer, der skal vises (alle)                                      |
| `--models`  | Vis modelbrugsopdeling (skjult som standard). Send et tal for at vise top N |
| `--project` | Filtre efter projekt (alle projekter, tom streng: nuværende projekt)        |

---

### export

Eksporter sessionsdata som JSON.

```bash
openstudier export [sessionID]
```

Hvis du ikke angiver et sessions-id, bliver du bedt om at vælge mellem tilgængelige sessioner.

---

### import

Importer sessionsdata fra en JSON fil eller OpenStudier del URL.

```bash
openstudier import <file>
```

Du kan importere fra en lokal fil eller en OpenStudier share URL.

```bash
openstudier import session.json
openstudier import https://opncd.ai/s/abc123
```

---

### web

Start en hovedløs OpenStudier-server med en webgrænseflade.

```bash
openstudier web
```

Dette starter en HTTP-server og åbner en webbrowser for at få adgang til OpenStudier via en webgrænseflade. Indstil `OPENSTUDIER_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `openstudier`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### acp

Start en ACP-server (Agent Client Protocol).

```bash
openstudier acp
```

Denne kommando starter en ACP-server, der kommunikerer via stdin/stdout ved hjælp af nd-JSON.

#### Flag

| Flag         | Beskrivelse           |
| ------------ | --------------------- |
| `--cwd`      | Arbejdsmappe          |
| `--port`     | Port at lytte på      |
| `--hostname` | Værtsnavn at lytte på |

---

### uninstall

Afinstaller OpenStudier og fjern alle relaterede filer.

```bash
openstudier uninstall
```

#### Flag

| Flag            | Kort | Beskrivelse                                      |
| --------------- | ---- | ------------------------------------------------ |
| `--keep-config` | `-c` | Se konfigurationsfiler                           |
| `--keep-data`   | `-d` | Gem sessionsdata og snapshots                    |
| `--dry-run`     |      | Vis, hvad der ville blive fjernet uden at fjerne |
| `--force`       | `-f` | Spring bekræftelsesspørgsmål over                |

---

### upgrade

Opdaterer OpenStudier til den nyeste version eller en specifik version.

```bash
openstudier upgrade [target]
```

For at opgradere til den nyeste version.

```bash
openstudier upgrade
```

For at opgradere til en bestemt version.

```bash
openstudier upgrade v0.1.48
```

#### upgrade

| Flag       | Kort | Beskrivelse                                                      |
| ---------- | ---- | ---------------------------------------------------------------- |
| `--method` | `-m` | Installationsmetoden, der blev brugt; curl, npm, pnpm, bun, brew |

---

## Globale flag

openstudier CLI tager følgende globale flag.

| Flag           | Kort | Beskrivelse                          |
| -------------- | ---- | ------------------------------------ |
| `--help`       | `-h` | Vis hjælp                            |
| `--version`    | `-v` | Udskriftsversionsnummer              |
| `--print-logs` |      | Udskriv logfiler til stderr          |
| `--log-level`  |      | Logniveau (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

OpenStudier kan konfigureres ved hjælp af miljøvariabler.

| Variabel                              | Skriv   | Beskrivelse                                                           |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `OPENSTUDIER_AUTO_SHARE`                 | boolean | Del automatisk session                                                |
| `OPENSTUDIER_GIT_BASH_PATH`              | string  | Sti til Git Bash eksekverbar på Windows                               |
| `OPENSTUDIER_CONFIG`                     | string  | Sti til konfigurationsfil                                             |
| `OPENSTUDIER_TUI_CONFIG`                 | string  | Sti til TUI-konfigurationsfil                                         |
| `OPENSTUDIER_CONFIG_DIR`                 | string  | Sti til konfigurationsmappe                                           |
| `OPENSTUDIER_CONFIG_CONTENT`             | string  | Indbygget json-konfigurationsindhold                                  |
| `OPENSTUDIER_DISABLE_AUTOUPDATE`         | boolean | Deaktiver automatisk opdateringskontrol                               |
| `OPENSTUDIER_DISABLE_PRUNE`              | boolean | Deaktiver beskæring af gamle data                                     |
| `OPENSTUDIER_DISABLE_TERMINAL_TITLE`     | boolean | Deaktiver automatisk opdatering af terminaltitel                      |
| `OPENSTUDIER_PERMISSION`                 | string  | Indbygget json-tilladelseskonfiguration                               |
| `OPENSTUDIER_DISABLE_DEFAULT_PLUGINS`    | boolean | Deaktiver standard plugins                                            |
| `OPENSTUDIER_DISABLE_LSP_DOWNLOAD`       | boolean | Deaktiver automatisk LSP-serverdownloads                              |
| `OPENSTUDIER_ENABLE_EXPERIMENTAL_MODELS` | boolean | Aktive eksperimentelle modeller                                       |
| `OPENSTUDIER_DISABLE_AUTOCOMPACT`        | boolean | Deaktiver automatisk kontekstkomprimering                             |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE`        | boolean | Deaktiver læsning fra `.claude` (prompt + færdigheder)                |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Deaktiver læsning `~/.claude/CLAUDE.md`                               |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Deaktiver indlæsning af `.claude/skills`                              |
| `OPENSTUDIER_DISABLE_MODELS_FETCH`       | boolean | Deaktivering af modeller fra eksterne kilder                          |
| `OPENSTUDIER_FAKE_VCS`                   | string  | Falsk VCS-udbyder til testformål                                      |
| `OPENSTUDIER_DISABLE_FILETIME_CHECK`     | boolean | Deaktiver filtidskontrol for optimering                               |
| `OPENSTUDIER_CLIENT`                     | string  | Klient-id (standard til `cli`)                                        |
| `OPENSTUDIER_ENABLE_EXA`                 | boolean | Aktiver Exa-websøgeværktøjer                                          |
| `OPENSTUDIER_SERVER_PASSWORD`            | string  | Aktiver grundlæggende godkendelse for `serve`/`web`                   |
| `OPENSTUDIER_SERVER_USERNAME`            | string  | Tilsidesæt grundlæggende godkendelsesbrugernavn (standard `openstudier`) |
| `OPENSTUDIER_MODELS_URL`                 | string  | Brugerdefineret URL til hentning af modelkonfiguration                |

---

### Eksperimentel

Disse miljøvariabler muliggør eksperimentelle funktioner, der kan ændres eller fjernes.

| Variabel                                        | Skriv   | Beskrivelse                                |
| ----------------------------------------------- | ------- | ------------------------------------------ |
| `OPENSTUDIER_EXPERIMENTAL`                         | boolean | Aktiver alle eksperimentelle funktioner    |
| `OPENSTUDIER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Aktiver ikonopdagelse                      |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Deaktiver kopi ved valg i TUI              |
| `OPENSTUDIER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | nummer  | Standard timeout for bash-kommandoer i ms  |
| `OPENSTUDIER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | nummer  | Maks. output-tokens for LLM-svar           |
| `OPENSTUDIER_EXPERIMENTAL_FILEWATCHER`             | boolean | Aktiver filovervågning for hele dir        |
| `OPENSTUDIER_EXPERIMENTAL_OXFMT`                   | boolean | Aktiver oxfmt formatter                    |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TOOL`                | boolean | Aktive eksperimenter LSP værktøj           |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Deaktiver filovervågning                   |
| `OPENSTUDIER_EXPERIMENTAL_EXA`                     | boolean | Aktive eksperimenter Exa-funktioner        |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TY`                  | boolean | Aktive forsøg LSP typekontrol              |
| `OPENSTUDIER_EXPERIMENTAL_MARKDOWN`                | boolean | Aktive eksperimentelle markdown-funktioner |
| `OPENSTUDIER_EXPERIMENTAL_PLAN_MODE`               | boolean | Aktiver plantilstand                       |
