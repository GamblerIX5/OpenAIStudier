---
title: CLI
description: Opcje i polecenia wiersza poleceń OpenStudier.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI OpenStudier domyślnie uruchamia [TUI](/docs/tui), gdy wywołasz je bez argumentów.

```bash
openstudier
```

CLI obsługuje też polecenia opisane na tej stronie, więc możesz używać OpenStudier programowo.

```bash
openstudier run "Explain how closures work in JavaScript"
```

---

### tui

Uruchamia interfejs terminalowy (TUI) OpenStudier.

```bash
openstudier [project]
```

#### Flagi

| Flaga        | Skrót | Opis                                                                    |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Kontynuuj ostatnią sesję                                                |
| `--session`  | `-s`  | Identyfikator sesji do kontynuowania                                    |
| `--fork`     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`) |
| `--prompt`   |       | Monit do użycia                                                         |
| `--model`    | `-m`  | Model do użycia w formacie dostawca/model                               |
| `--agent`    |       | Agent do użycia                                                         |
| `--port`     |       | Port do nasłuchiwania                                                   |
| `--hostname` |       | Nazwa hosta, do której należy się powiązać                              |

---

## Polecenia

Interfejs CLI OpenStudier zawiera także dodatkowe polecenia.

---

### agent

Zarządzaj agentami OpenStudier.

```bash
openstudier agent [command]
```

---

### attach

Dołącz terminal do już działającego serwera backendu OpenStudier uruchomionego za pomocą `serve` lub `web`.

```bash
openstudier attach [url]
```

Pozwala to połączyć TUI ze zdalnym backendem OpenStudier. Na przykład:

```bash
# Start the backend server for web/mobile access
openstudier web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
openstudier attach http://10.20.30.40:4096
```

#### Flagi

| Flaga       | Skrót | Opis                                    |
| ----------- | ----- | --------------------------------------- |
| `--dir`     |       | Katalog roboczy, w którym uruchomić TUI |
| `--session` | `-s`  | Identyfikator sesji do kontynuowania    |

---

#### create

Utwórz nowego agenta z niestandardową konfiguracją.

```bash
openstudier agent create
```

To polecenie poprowadzi Cię przez proces tworzenia nowego agenta z niestandardowym promptem systemowym i konfiguracją narzędzi.

---

#### list

Wyświetl listę wszystkich agentów.

```bash
openstudier agent list
```

---

### auth

Zarządzaj danymi uwierzytelniającymi i logowaniem dla dostawców.

```bash
openstudier auth [command]
```

---

#### login

OpenStudier integruje się z wieloma dostawcami. Możesz użyć `openstudier auth login`, aby skonfigurować klucz API dla dowolnego obsługiwanego dostawcy. Klucze są bezpiecznie przechowywane w `~/.local/share/openstudier/auth.json`.

```bash
openstudier auth login
```

Podczas uruchamiania OpenStudier ładuje dostawców z pliku poświadczeń, a także ze zmiennych środowiskowych lub pliku `.env` w Twoim projekcie.

---

#### list

Wyświetl listę wszystkich uwierzytelnionych dostawców przechowywanych w pliku poświadczeń.

```bash
openstudier auth list
```

Lub skrócona wersja:

```bash
openstudier auth ls
```

---

#### logout

Wyloguj się z dostawcy, usuwając go z pliku poświadczeń.

```bash
openstudier auth logout
```

---

### github

Zarządzaj agentem GitHub w celu automatyzacji repozytorium.

```bash
openstudier github [command]
```

---

#### install

Zainstaluj agenta GitHub w swoim repozytorium.

```bash
openstudier github install
```

Spowoduje to utworzenie pliku workflow GitHub Actions i poprowadzi Cię przez proces konfiguracji. [Dowiedz się więcej](/docs/github).

---

#### run

Uruchom agenta na GitHubie. Zwykle używane w GitHub Actions.

```bash
openstudier github run
```

##### Flagi

| Flaga     | Opis                                     |
| --------- | ---------------------------------------- |
| `--event` | Zdarzenie GitHub, które wyzwoliło agenta |
| `--token` | Osobisty token dostępu GitHub            |

---

### mcp

Zarządzaj serwerami Model Context Protocol (MCP).

```bash
openstudier mcp [command]
```

---

#### add

Dodaj serwer MCP do swojej konfiguracji.

```bash
openstudier mcp add
```

To polecenie poprowadzi Cię przez proces dodawania lokalnego lub zdalnego serwera MCP.

---

#### list

Wyświetl listę wszystkich skonfigurowanych serwerów MCP i ich status.

```bash
openstudier mcp list
```

Lub skrócona wersja:

```bash
openstudier mcp ls
```

---

#### auth

Uwierzytelnij się w serwerze MCP z włączoną funkcją OAuth.

```bash
openstudier mcp auth [name]
```

Jeśli nie podasz nazwy serwera, zostaniesz poproszony o wybranie jednego z serwerów wymagających uwierzytelnienia.

Możesz także wyświetlić listę serwerów obsługujących OAuth i ich status uwierzytelnienia.

```bash
openstudier mcp auth list
```

Lub skrócona wersja:

```bash
openstudier mcp auth ls
```

---

#### logout

Usuń tokeny OAuth dla serwera MCP.

```bash
openstudier mcp logout [name]
```

---

#### debug

Debuguj problemy z połączeniem OAuth dla serwera MCP.

```bash
openstudier mcp debug <name>
```

---

### models

Wyświetl listę modeli ze wszystkich skonfigurowanych dostawców.

```bash
openstudier models [provider]
```

Pokazuje modele dostępne do użycia w `provider/model`.

Jest to identyfikator modelu, którego używasz w [konfiguracji](/docs/config).

Opcjonalnie możesz podać identyfikator dostawcy, aby filtrować modele według tego dostawcy.

```bash
openstudier models anthropic
```

#### Flagi

| Flaga       | Opis                                                                            |
| ----------- | ------------------------------------------------------------------------------- |
| `--refresh` | Odśwież pamięć podręczną modeli                                                 |
| `--verbose` | Bardziej szczegółowe dane wyjściowe modelu (zawiera metadane, takie jak koszty) |

Użyj flagi `--refresh`, aby zaktualizować listę modeli w pamięci podręcznej. Jest to przydatne, gdy dostawca dodał nowe modele, które chcesz zobaczyć w OpenStudier.

```bash
openstudier models --refresh
```

---

### run

Uruchom OpenStudier w trybie nieinteraktywnym, przekazując bezpośrednio prompt.

```bash
openstudier run [message..]
```

Jest to przydatne do tworzenia skryptów, automatyzacji lub gdy chcesz wysłać szybkie zapytanie bez uruchamiania TUI. Na przykład:

```bash "openstudier run"
openstudier run Explain the use of context in Go
```

Można również użyć tego z działającym `openstudier serve`, aby uniknąć zimnego startu serwera MCP przy każdym uruchomieniu:

```bash
# Start a headless server in one terminal
openstudier serve

# In another terminal, run commands that attach to it
openstudier run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagi

| Flaga        | Skrót | Opis                                                                    |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | Polecenie do uruchomienia, reszta to argumenty                          |
| `--continue` | `-c`  | Kontynuuj ostatnią sesję                                                |
| `--session`  | `-s`  | Identyfikator sesji do kontynuowania                                    |
| `--fork`     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`) |
| `--share`    |       | Udostępnij sesję po zakończeniu                                         |
| `--model`    | `-m`  | Model do użycia w formacie dostawca/model                               |
| `--agent`    |       | Agent do użycia                                                         |
| `--file`     | `-f`  | Pliki do załączenia do wiadomości                                       |
| `--format`   |       | Format wyjściowy: `default` (sformatowany) lub `json` (surowy JSON)     |
| `--title`    |       | Tytuł sesji (jeśli nie podano, zostanie wygenerowany z promptu)         |
| `--attach`   |       | Dołącz do działającego serwera OpenStudier (np. http://localhost:4096)     |
| `--port`     |       | Port dla serwera lokalnego (domyślnie losowy)                           |

---

### serve

Uruchom serwer OpenStudier (bez interfejsu) w celu uzyskania dostępu do API. Pełny opis API HTTP znajduje się w [dokumentacji serwera](/docs/server).

```bash
openstudier serve
```

Uruchamia to serwer HTTP, który zapewnia dostęp do API OpenStudier bez interfejsu TUI. Ustaw `OPENSTUDIER_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `openstudier`).

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |
| `--mdns`     | Włącz wykrywanie mDNS                      |
| `--cors`     | Dodatkowe dozwolone źródła CORS            |

---

### session

Zarządzaj sesjami OpenStudier.

```bash
openstudier session [command]
```

---

#### list

Lista wszystkich sesji OpenStudier.

```bash
openstudier session list
```

##### Flagi

| Flaga         | Skrót | Opis                                       |
| ------------- | ----- | ------------------------------------------ |
| `--max-count` | `-n`  | Ogranicz do ostatnich N sesji              |
| `--format`    |       | Format wyjściowy: tabela lub json (tabela) |

---

### stats

Pokaż statystyki wykorzystania tokenów i sesji OpenStudier.

```bash
openstudier stats
```

#### Flagi

| Flaga       | Opis                                                                                  |
| ----------- | ------------------------------------------------------------------------------------- |
| `--days`    | Pokaż statystyki z ostatnich N dni (domyślnie: cały czas)                             |
| `--tools`   | Pokaż użycie poszczególnych narzędzi (domyślnie: wszystkie)                           |
| `--models`  | Pokaż podział na modele (domyślnie ukryty). Podaj liczbę, aby pokazać N najczęstszych |
| `--project` | Filtruj według projektu (domyślnie: wszystkie projekty, pusty ciąg: bieżący projekt)  |

---

### export

Eksportuj dane sesji jako JSON.

```bash
openstudier export [sessionID]
```

Jeśli nie podasz identyfikatora sesji, zostaniesz poproszony o wybranie jednej z ostatnich sesji.

---

### import

Importuj dane sesji z pliku JSON lub adresu URL udostępniania OpenStudier.

```bash
openstudier import <file>
```

Możesz importować z pliku lokalnego lub adresu URL.

```bash
openstudier import session.json
openstudier import https://opncd.ai/s/abc123
```

---

### web

Uruchom serwer OpenStudier z interfejsem internetowym.

```bash
openstudier web
```

Uruchamia to serwer HTTP i udostępnia OpenStudier przez interfejs przeglądarkowy. Ustaw `OPENSTUDIER_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `openstudier`).

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |
| `--mdns`     | Włącz wykrywanie mDNS                      |
| `--cors`     | Dodatkowe dozwolone źródła CORS            |

---

### acp

Uruchom serwer ACP (Agent Client Protocol).

```bash
openstudier acp
```

Uruchamia serwer ACP, który komunikuje się przez stdin/stdout przy użyciu JSON-RPC.

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--cwd`      | Katalog roboczy                            |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |

---

### uninstall

Odinstaluj OpenStudier i usuń wszystkie powiązane pliki.

```bash
openstudier uninstall
```

#### Flagi

| Flaga           | Skrót | Opis                          |
| --------------- | ----- | ----------------------------- |
| `--keep-config` | `-c`  | Zachowaj pliki konfiguracyjne |
| `--keep-data`   | `-d`  | Zachowaj dane sesji i migawki |
| `--dry-run`     |       | Pokaż co zostanie usunięte    |
| `--force`       | `-f`  | Pomiń monity o potwierdzenie  |

---

### upgrade

Aktualizuj OpenStudier do najnowszej lub określonej wersji.

```bash
openstudier upgrade [target]
```

Aby zaktualizować do najnowszej wersji:

```bash
openstudier upgrade
```

Aby zaktualizować do konkretnej wersji:

```bash
openstudier upgrade v0.1.48
```

#### Flagi

| Flaga      | Skrót | Opis                                                |
| ---------- | ----- | --------------------------------------------------- |
| `--method` | `-m`  | Wymuś metodę instalacji: curl, npm, pnpm, bun, brew |

---

## Flagi globalne

Interfejs CLI OpenStudier przyjmuje następujące flagi globalne dla każdego polecenia.

| Flaga          | Skrót | Opis                                        |
| -------------- | ----- | ------------------------------------------- |
| `--help`       | `-h`  | Wyświetl pomoc                              |
| `--version`    | `-v`  | Wydrukuj numer wersji                       |
| `--print-logs` |       | Drukuj logi na stderr                       |
| `--log-level`  |       | Poziom logowania (DEBUG, INFO, WARN, ERROR) |

---

## Zmienne środowiskowe

OpenStudier można skonfigurować za pomocą zmiennych środowiskowych.

| Zmienna                               | Typ     | Opis                                                        |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `OPENSTUDIER_AUTO_SHARE`                 | boolean | Automatycznie udostępniaj sesje                             |
| `OPENSTUDIER_GIT_BASH_PATH`              | string  | Ścieżka do pliku wykonywalnego Git Bash w systemie Windows  |
| `OPENSTUDIER_CONFIG`                     | string  | Ścieżka do pliku konfiguracyjnego                           |
| `OPENSTUDIER_TUI_CONFIG`                 | string  | Ścieżka do pliku konfiguracyjnego TUI                       |
| `OPENSTUDIER_CONFIG_DIR`                 | string  | Ścieżka do katalogu konfiguracyjnego                        |
| `OPENSTUDIER_CONFIG_CONTENT`             | string  | Treść konfiguracji JSON (inline)                            |
| `OPENSTUDIER_DISABLE_AUTOUPDATE`         | boolean | Wyłącz automatyczne sprawdzanie aktualizacji                |
| `OPENSTUDIER_DISABLE_PRUNE`              | boolean | Wyłącz czyszczenie starych wyników (pruning)                |
| `OPENSTUDIER_DISABLE_TERMINAL_TITLE`     | boolean | Wyłącz automatyczne ustawianie tytułu terminala             |
| `OPENSTUDIER_PERMISSION`                 | string  | Konfiguracja uprawnień w JSON (inline)                      |
| `OPENSTUDIER_DISABLE_DEFAULT_PLUGINS`    | boolean | Wyłącz domyślne wtyczki                                     |
| `OPENSTUDIER_DISABLE_LSP_DOWNLOAD`       | boolean | Wyłącz automatyczne pobieranie serwerów LSP                 |
| `OPENSTUDIER_ENABLE_EXPERIMENTAL_MODELS` | boolean | Włącz modele eksperymentalne                                |
| `OPENSTUDIER_DISABLE_AUTOCOMPACT`        | boolean | Wyłącz automatyczne kompaktowanie kontekstu                 |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE`        | boolean | Wyłącz integrację z `.claude` (prompt + skills)             |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Wyłącz czytanie `~/.claude/CLAUDE.md`                       |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Wyłącz ładowanie `.claude/skills`                           |
| `OPENSTUDIER_DISABLE_MODELS_FETCH`       | boolean | Wyłącz pobieranie modeli ze źródeł zewnętrznych             |
| `OPENSTUDIER_FAKE_VCS`                   | string  | Fałszywy dostawca VCS do celów testowych                    |
| `OPENSTUDIER_DISABLE_FILETIME_CHECK`     | boolean | Wyłącz sprawdzanie czasu modyfikacji plików (optymalizacja) |
| `OPENSTUDIER_CLIENT`                     | string  | Identyfikator klienta (domyślnie `cli`)                     |
| `OPENSTUDIER_ENABLE_EXA`                 | boolean | Włącz narzędzie wyszukiwania internetowego Exa              |
| `OPENSTUDIER_SERVER_PASSWORD`            | string  | Włącz uwierzytelnianie podstawowe dla `serve`/`web`         |
| `OPENSTUDIER_SERVER_USERNAME`            | string  | Nazwa użytkownika do autoryzacji (domyślnie `openstudier`)     |
| `OPENSTUDIER_MODELS_URL`                 | string  | Niestandardowy adres URL do pobierania konfiguracji modeli  |

---

### Eksperymentalne

Te zmienne włączają funkcje eksperymentalne, które mogą ulec zmianie lub zostać usunięte.

| Zmienna                                         | Typ     | Opis                                         |
| ----------------------------------------------- | ------- | -------------------------------------------- |
| `OPENSTUDIER_EXPERIMENTAL`                         | boolean | Włącz wszystkie funkcje eksperymentalne      |
| `OPENSTUDIER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Włącz wykrywanie ikon                        |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Wyłącz kopiowanie przy zaznaczaniu w TUI     |
| `OPENSTUDIER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Domyślny limit czasu dla narzędzia bash w ms |
| `OPENSTUDIER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksymalne tokeny wyjściowe dla LLM          |
| `OPENSTUDIER_EXPERIMENTAL_FILEWATCHER`             | boolean | Włącz obserwatora plików dla całego katalogu |
| `OPENSTUDIER_EXPERIMENTAL_OXFMT`                   | boolean | Włącz formater oxfmt                         |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TOOL`                | boolean | Włącz eksperymentalne narzędzie LSP          |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Wyłącz obserwatora plików                    |
| `OPENSTUDIER_EXPERIMENTAL_EXA`                     | boolean | Włącz funkcje eksperymentalne Exa            |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TY`                  | boolean | Włącz eksperymentalne sprawdzanie typów LSP  |
| `OPENSTUDIER_EXPERIMENTAL_MARKDOWN`                | boolean | Włącz funkcje eksperymentalne Markdown       |
| `OPENSTUDIER_EXPERIMENTAL_PLAN_MODE`               | boolean | Włącz tryb planowania                        |
