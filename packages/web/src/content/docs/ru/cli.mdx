---
title: CLI
description: Параметры и команда openstudier CLI.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI openstudier по умолчанию запускает [TUI](/docs/tui) при запуске без каких-либо аргументов.

```bash
openstudier
```

Но он также принимает команды, описанные на этой странице. Это позволяет вам программно взаимодействовать с openstudier.

```bash
openstudier run "Explain how closures work in JavaScript"
```

---

### tui

Запустите TUI openstudier.

```bash
openstudier [project]
```

#### Флаги

| Флаг         | Короткий | Описание                                                                      |
| ------------ | -------- | ----------------------------------------------------------------------------- |
| `--continue` | `-c`     | Продолжить последний сеанс                                                    |
| `--session`  | `-s`     | Идентификатор сеанса для продолжения                                          |
| `--fork`     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`) |
| `--prompt`   |          | Промпт для использования                                                      |
| `--model`    | `-m`     | Модель для использования в виде поставщика/модели.                            |
| `--agent`    |          | Агент для использования                                                       |
| `--port`     |          | Порт для прослушивания                                                        |
| `--hostname` |          | Имя хоста для прослушивания                                                   |

---

## Команды

CLI openstudier также имеет следующие команды.

---

### agent

Управляйте агентами для openstudier.

```bash
openstudier agent [command]
```

---

### attach

Подключите терминал к уже работающему внутреннему серверу openstudier, запущенному с помощью команд `serve` или `web`.

```bash
openstudier attach [url]
```

Это позволяет использовать TUI с удаленным сервером openstudier. Например:

```bash
# Start the backend server for web/mobile access
openstudier web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
openstudier attach http://10.20.30.40:4096
```

#### Флаги

| Флаг        | Короткий | Описание                             |
| ----------- | -------- | ------------------------------------ |
| `--dir`     |          | Рабочий каталог для запуска TUI      |
| `--session` | `-s`     | Идентификатор сеанса для продолжения |

---

#### create

Создайте нового агента с пользовательской конфигурацией.

```bash
openstudier agent create
```

Эта команда поможет вам создать новый агент с настраиваемой системной подсказкой и настройкой инструмента.

---

#### list

Перечислите всех доступных агентов.

```bash
openstudier agent list
```

---

### auth

Команда для управления учетными данными и входом в систему для провайдеров.

```bash
openstudier auth [command]
```

---

#### login

openstudier использует список провайдеров с [Models.dev](https://models.dev), поэтому вы можете использовать `openstudier auth login` для настройки ключей API для любого поставщика, которого вы хотите использовать. Это хранится в `~/.local/share/openstudier/auth.json`.

```bash
openstudier auth login
```

Когда openstudier запускается, он загружает поставщиков из файла учетных данных. И если в ваших средах определены какие-либо ключи или файл `.env` в вашем проекте.

---

#### list

Перечисляет всех проверенных поставщиков, которые хранятся в файле учетных данных.

```bash
openstudier auth list
```

Или короткая версия.

```bash
openstudier auth ls
```

---

#### logout

Выключает вас из провайдера, удаляя его из файла учетных данных.

```bash
openstudier auth logout
```

---

### github

Управляйте агентом GitHub для автоматизации репозитория.

```bash
openstudier github [command]
```

---

#### install

Установите агент GitHub в свой репозиторий.

```bash
openstudier github install
```

Это настроит необходимый рабочий процесс GitHub Actions и проведет вас через процесс настройки. [Подробнее](/docs/github).

---

#### run

Запустите агент GitHub. Обычно это используется в действиях GitHub.

```bash
openstudier github run
```

##### Флаги

| Флаг      | Описание                                      |
| --------- | --------------------------------------------- |
| `--event` | Имитирующее событие GitHub для запуска агента |
| `--token` | Токен личного доступа GitHub                  |

---

### mcp

Управляйте серверами протокола контекста модели.

```bash
openstudier mcp [command]
```

---

#### add

Добавьте сервер MCP в свою конфигурацию.

```bash
openstudier mcp add
```

Эта команда поможет вам добавить локальный или удаленный сервер MCP.

---

#### list

Перечислите все настроенные серверы MCP и состояние их подключения.

```bash
openstudier mcp list
```

Или используйте короткую версию.

```bash
openstudier mcp ls
```

---

#### auth

Аутентификация с помощью сервера MCP с поддержкой OAuth.

```bash
openstudier mcp auth [name]
```

Если вы не укажете имя сервера, вам будет предложено выбрать один из доступных серверов с поддержкой OAuth.

Вы также можете перечислить серверы с поддержкой OAuth и их статус аутентификации.

```bash
openstudier mcp auth list
```

Или используйте короткую версию.

```bash
openstudier mcp auth ls
```

---

#### logout

Удалите учетные данные OAuth для сервера MCP.

```bash
openstudier mcp logout [name]
```

---

#### debug

Отладка проблем с подключением OAuth для сервера MCP.

```bash
openstudier mcp debug <name>
```

---

### models

Перечислите все доступные модели от настроенных поставщиков.

```bash
openstudier models [provider]
```

Эта команда отображает все модели, доступные у настроенных вами поставщиков, в формате `provider/model`.

Это полезно для определения точного названия модели, которое будет использоваться в [вашем config](/docs/config/).

При желании вы можете передать идентификатор поставщика, чтобы фильтровать модели по этому поставщику.

```bash
openstudier models anthropic
```

#### Флаги

| Флаг        | Описание                                                                          |
| ----------- | --------------------------------------------------------------------------------- |
| `--refresh` | Обновите кеш моделей на сайте models.dev.                                         |
| `--verbose` | Используйте более подробный вывод модели (включая метаданные, такие как затраты). |

Используйте флаг `--refresh` для обновления списка кэшированных моделей. Это полезно, когда к поставщику добавлены новые модели и вы хотите увидеть их в openstudier.

```bash
openstudier models --refresh
```

---

### run

Запустите openstudier в неинтерактивном режиме, передав приглашение напрямую.

```bash
openstudier run [message..]
```

Это полезно для создания сценариев, автоматизации или когда вам нужен быстрый ответ без запуска полного TUI. Например.

```bash "openstudier run"
openstudier run Explain the use of context in Go
```

Вы также можете подключиться к работающему экземпляру `openstudier serve`, чтобы избежать холодной загрузки сервера MCP при каждом запуске:

```bash
# Start a headless server in one terminal
openstudier serve

# In another terminal, run commands that attach to it
openstudier run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Флаги

| Флаг         | Короткий | Описание                                                                         |
| ------------ | -------- | -------------------------------------------------------------------------------- |
| `--command`  |          | Команда для запуска, используйте сообщение для аргументов                        |
| `--continue` | `-c`     | Продолжить последний сеанс                                                       |
| `--session`  | `-s`     | Идентификатор сеанса для продолжения                                             |
| `--fork`     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`)    |
| `--share`    |          | Поделиться сеансом                                                               |
| `--model`    | `-m`     | Модель для использования в виде поставщика/модели.                               |
| `--agent`    |          | Агент для использования                                                          |
| `--file`     | `-f`     | Файл(ы) для прикрепления к сообщению                                             |
| `--format`   |          | Формат: по умолчанию (отформатированный) или json (необработанные события JSON). |
| `--title`    |          | Название сеанса (использует усеченное приглашение, если значение не указано)     |
| `--attach`   |          | Подключитесь к работающему серверу openstudier (например, http://localhost:4096)    |
| `--port`     |          | Порт локального сервера (по умолчанию случайный порт)                            |

---

### serve

Запустите автономный сервер openstudier для доступа к API. Полный HTTP-интерфейс можно найти в [server docs](/docs/server).

```bash
openstudier serve
```

При этом запускается HTTP-сервер, который обеспечивает доступ API к функциям openstudier без интерфейса TUI. Установите `OPENSTUDIER_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `openstudier`).

#### Флаги

| Флаг         | Описание                                                      |
| ------------ | ------------------------------------------------------------- |
| `--port`     | Порт для прослушивания                                        |
| `--hostname` | Имя хоста для прослушивания                                   |
| `--mdns`     | Включить обнаружение mDNS                                     |
| `--cors`     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### session

Управляйте сессиями openstudier.

```bash
openstudier session [command]
```

---

#### list

Перечислите все сеансы openstudier.

```bash
openstudier session list
```

##### Флаги

| Флаг          | Короткий | Описание                                  |
| ------------- | -------- | ----------------------------------------- |
| `--max-count` | `-n`     | Ограничить N последних сеансов.           |
| `--format`    |          | Формат вывода: таблица или json (таблица) |

---

### stats

Покажите статистику использования токенов и затрат для ваших сеансов openstudier.

```bash
openstudier stats
```

#### Флаги

| Флаг        | Описание                                                                                                   |
| ----------- | ---------------------------------------------------------------------------------------------------------- |
| `--days`    | Показать статистику за последние N дней (все время)                                                        |
| `--tools`   | Количество инструментов для отображения (все)                                                              |
| `--models`  | Показать разбивку по использованию модели (по умолчанию скрыто). Передайте номер, чтобы показать верхнюю N |
| `--project` | Фильтровать по проекту (все проекты, пустая строка: текущий проект)                                        |

---

### export

Экспортируйте данные сеанса в формате JSON.

```bash
openstudier export [sessionID]
```

Если вы не укажете идентификатор сеанса, вам будет предложено выбрать один из доступных сеансов.

---

### import

Импортируйте данные сеанса из файла JSON или URL-адреса общего ресурса openstudier.

```bash
openstudier import <file>
```

Вы можете импортировать из локального файла или URL-адреса общего ресурса openstudier.

```bash
openstudier import session.json
openstudier import https://opncd.ai/s/abc123
```

---

### web

Запустите автономный сервер openstudier с веб-интерфейсом.

```bash
openstudier web
```

При этом запускается HTTP-сервер и открывается веб-браузер для доступа к openstudier через веб-интерфейс. Установите `OPENSTUDIER_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `openstudier`).

#### Флаги

| Флаг         | Описание                                                      |
| ------------ | ------------------------------------------------------------- |
| `--port`     | Порт для прослушивания                                        |
| `--hostname` | Имя хоста для прослушивания                                   |
| `--mdns`     | Включить обнаружение mDNS                                     |
| `--cors`     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### acp

Запустите сервер ACP (агент-клиентский протокол).

```bash
openstudier acp
```

Эта команда запускает сервер ACP, который обменивается данными через stdin/stdout с использованием nd-JSON.

#### Флаги

| Флаг         | Описание                    |
| ------------ | --------------------------- |
| `--cwd`      | Рабочий каталог             |
| `--port`     | Порт для прослушивания      |
| `--hostname` | Имя хоста для прослушивания |

---

### uninstall

Удалите openstudier и удалите все связанные файлы.

```bash
openstudier uninstall
```

#### Флаги

| Флаг            | Короткий | Описание                                   |
| --------------- | -------- | ------------------------------------------ |
| `--keep-config` | `-c`     | Сохраняйте файлы конфигурации              |
| `--keep-data`   | `-d`     | Храните данные сеанса и снимки             |
| `--dry-run`     |          | Покажите, что было бы удалено без удаления |
| `--force`       | `-f`     | Пропустить запросы подтверждения           |

---

### upgrade

Обновляет openstudier до последней версии или определенной версии.

```bash
openstudier upgrade [target]
```

Чтобы обновиться до последней версии.

```bash
openstudier upgrade
```

Для обновления до определенной версии.

```bash
openstudier upgrade v0.1.48
```

#### Флаги

| Флаг       | Короткий | Описание                                                  |
| ---------- | -------- | --------------------------------------------------------- |
| `--method` | `-m`     | Используемый метод установки: local, npm, pnpm, bun, brew |

---

## Глобальные флаги

CLI openstudier принимает следующие глобальные флаги.

| Флаг           | Короткий | Описание                                   |
| -------------- | -------- | ------------------------------------------ |
| `--help`       | `-h`     | Отобразить справку                         |
| `--version`    | `-v`     | Распечатать номер версии                   |
| `--print-logs` |          | Печать журналов в stderr                   |
| `--log-level`  |          | Уровень журнала (DEBUG, INFO, WARN, ERROR) |

---

## Переменные среды

openstudier можно настроить с помощью переменных среды.

| Переменная                            | Тип                 | Описание                                                                         |
| ------------------------------------- | ------------------- | -------------------------------------------------------------------------------- |
| `OPENSTUDIER_AUTO_SHARE`                 | логическое значение | Автоматически делиться сеансами                                                  |
| `OPENSTUDIER_GIT_BASH_PATH`              | строка              | Путь к исполняемому файлу Git Bash в Windows                                     |
| `OPENSTUDIER_CONFIG`                     | строка              | Путь к файлу конфигурации                                                        |
| `OPENSTUDIER_TUI_CONFIG`                 | строка              | Путь к файлу конфигурации TUI                                                    |
| `OPENSTUDIER_CONFIG_DIR`                 | строка              | Путь к каталогу конфигурации                                                     |
| `OPENSTUDIER_CONFIG_CONTENT`             | строка              | Встроенное содержимое конфигурации json                                          |
| `OPENSTUDIER_DISABLE_AUTOUPDATE`         | логическое значение | Отключить автоматическую проверку обновлений                                     |
| `OPENSTUDIER_DISABLE_PRUNE`              | логическое значение | Отключить удаление старых данных                                                 |
| `OPENSTUDIER_DISABLE_TERMINAL_TITLE`     | логическое значение | Отключить автоматическое обновление заголовка терминала                          |
| `OPENSTUDIER_PERMISSION`                 | строка              | Встроенная конфигурация разрешений json                                          |
| `OPENSTUDIER_DISABLE_DEFAULT_PLUGINS`    | логическое значение | Отключить плагины по умолчанию                                                   |
| `OPENSTUDIER_DISABLE_LSP_DOWNLOAD`       | логическое значение | Отключить автоматическую загрузку LSP-сервера                                    |
| `OPENSTUDIER_ENABLE_EXPERIMENTAL_MODELS` | логическое значение | Включить экспериментальные модели                                                |
| `OPENSTUDIER_DISABLE_AUTOCOMPACT`        | логическое значение | Отключить автоматическое сжатие контекста                                        |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE`        | логическое значение | Отключить чтение из `.claude` (подсказка + навыки)                               |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_PROMPT` | логическое значение | Отключить чтение `~/.claude/CLAUDE.md`                                           |
| `OPENSTUDIER_DISABLE_CLAUDE_CODE_SKILLS` | логическое значение | Отключить загрузку `.claude/skills`                                              |
| `OPENSTUDIER_DISABLE_MODELS_FETCH`       | логическое значение | Отключить получение моделей из удаленных источников                              |
| `OPENSTUDIER_FAKE_VCS`                   | строка              | Поддельный поставщик VCS для целей тестирования                                  |
| `OPENSTUDIER_DISABLE_FILETIME_CHECK`     | логическое значение | Отключить проверку времени файла для оптимизации                                 |
| `OPENSTUDIER_CLIENT`                     | строка              | Идентификатор клиента (по умолчанию `cli`)                                       |
| `OPENSTUDIER_ENABLE_EXA`                 | логическое значение | Включить инструменты веб-поиска Exa                                              |
| `OPENSTUDIER_SERVER_PASSWORD`            | строка              | Включить базовую аутентификацию для `serve`/`web`                                |
| `OPENSTUDIER_SERVER_USERNAME`            | строка              | Переопределить имя пользователя базовой аутентификации (по умолчанию `openstudier`) |
| `OPENSTUDIER_MODELS_URL`                 | строка              | Пользовательский URL-адрес для получения конфигурации модели                     |

---

### Экспериментальные функции

Эти переменные среды позволяют использовать экспериментальные функции, которые могут быть изменены или удалены.

| Переменная                                      | Тип                 | Описание                                               |
| ----------------------------------------------- | ------------------- | ------------------------------------------------------ |
| `OPENSTUDIER_EXPERIMENTAL`                         | логическое значение | Включить все экспериментальные функции                 |
| `OPENSTUDIER_EXPERIMENTAL_ICON_DISCOVERY`          | логическое значение | Включить обнаружение значков                           |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | логическое значение | Отключить копирование при выборе в TUI                 |
| `OPENSTUDIER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | число               | Таймаут по умолчанию для команд bash в мс              |
| `OPENSTUDIER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | число               | Максимальное количество токенов вывода для ответов LLM |
| `OPENSTUDIER_EXPERIMENTAL_FILEWATCHER`             | логическое значение | Включить просмотр файлов для всего каталога            |
| `OPENSTUDIER_EXPERIMENTAL_OXFMT`                   | логическое значение | Включить форматтер oxfmt                               |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TOOL`                | логическое значение | Включить экспериментальный инструмент LSP              |
| `OPENSTUDIER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | логическое значение | Отключить просмотрщик файлов                           |
| `OPENSTUDIER_EXPERIMENTAL_EXA`                     | логическое значение | Включить экспериментальные функции Exa                 |
| `OPENSTUDIER_EXPERIMENTAL_LSP_TY`                  | логическое значение | Включить экспериментальную проверку типа LSP           |
| `OPENSTUDIER_EXPERIMENTAL_MARKDOWN`                | логическое значение | Включить экспериментальные функции Markdown            |
| `OPENSTUDIER_EXPERIMENTAL_PLAN_MODE`               | логическое значение | Включить режим плана                                   |
